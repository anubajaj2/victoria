<mvc:View
	controllerName="victoria.controller.Products"
	xmlns="sap.m"
	xmlns:core="sap.ui.core"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:semantic="sap.m.semantic"
	xmlns:f="sap.ui.layout.form"
	xmlns:smartField="sap.ui.comp.smartfield"
	xmlns:footerbar="sap.ushell.ui.footerbar"
	xmlns:app="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1">

	<semantic:FullscreenPage
		id="products"
		navButtonPress="onNavBack"
		showNavButton="true"
		title="Manage Products"
		busy="{objectView>/busy}"
		busyIndicatorDelay="{objectView>/delay}">

		<semantic:content>
		<f:SimpleForm
			minWidth="1024"
			maxContainerCols="2"
			editable="true"
			layout="ResponsiveGridLayout"
			labelSpanL="3"
			labelSpanM="3"
			emptySpanL="4"
			emptySpanM="4"
			columnsL="1"
			columnsM="1"
			class="editableForm">
			<f:content>
 			    <Label text="Product Code" labelFor="productInput" required="true"/>
				<!-- <Input
					id="productInput"
					type="Text"
					showSuggestion="true"
					value="{productModel>/ProductCode}"
					suggestionItems="{/Products}"
					suggestionItemSelected="selectedProduct">
					<suggestionItems>
						<core:Item text="{ProductCode}" key="{Id}"/>
					</suggestionItems>
				</Input> -->
				<ComboBox value="{productModel>/ProductCode}" change="productCodeCheck"
	items="{productModelInfo>/results}" valueState="{dataModel>/ProductCodeState}" valueStateText="Product Code cannot be blank"
											enabled="{viewModel>/codeEnabled}">
	<core:Item key="{productModelInfo>ProductCode}" text="{productModelInfo>ProductCode}" />
</ComboBox>

<Label text="Product Name"/>
<Input
	id="idProductName"
	type="Text"
	value="{productModel>/ProductName}">
</Input>


				<Label text="Type"/>
				<!-- <Select
					id="idDescription"
					selectedKey="{productModel>/Description}"
					items="{
						path: 'fixed>/items'
					}">
					<core:Item key="{fixed>text}" text="{fixed>text}" />
				</Select> -->

				<ComboBox
					id="idType"
					type="Text"
					selectionChange="onTypeChange"
					value="{productModel>/Type}"
					items="{
						path: 'fixed>/items'
					}">
					<core:Item key="{fixed>text}" text="{fixed>text}" />
				</ComboBox>

				<Label text = "Karat" required="false"/>
				<Select
					id="idKarat"
					change="onSelectChange"
					 enabled="{viewModel>/typeEnabled}"
					 selectedKey= "{/Karat/0/items}"
					items="{
						path: 'Karat>/items'
					}">
					<core:Item key="{Karat>text}" text="{Karat>text}" />
				</Select>


				<Label text="Hindi Name"/>
				<Input
					id="idHindi"
					type="Text"
					value="{productModel>/HindiName}"
					class = "hinditext">
				</Input>

				<Label text="Tunch"/>
				<Input
					id="idStandardCost"
					type="Number"
					change="onChange"
					value="{productModel>/Tunch}">
				</Input>

				<Label text="Wastage"/>
				<Input
					id="idReorderLevel"
					change="onChange"
					type="Number"
					value="{productModel>/Wastage}">
				</Input>

				<Label text="Customer Tunch"/>
				<Input
					id="idListPrice"
					type="Decimal"
					value="{productModel>/CustomerTunch}">
				</Input>

				<Label text="Alert Quantity"/>
				<Input
					id="idTargetLevel"
					type="Number"
					value="{productModel>/AlertQuantity}">
				</Input>

				<Label text="Making"/>
				<Input
					id="idMinimumReorderQuantity"
					type="Number"
					value="{productModel>/Making}">
				</Input>

				<Label text="Category"/>
				<ComboBox
					id="idCategory"
					type="Text"
					value="{productModel>/Category}"
					items="{
						path: 'per>/items'
					}">
					<core:Item key="{per>text}" text="{per>text}" />
				</ComboBox>

				<Label text="MakingD"/>
				<!-- <Select
					id="idQuantityPerUnit"
					selectedKey="{productModel>/QuantityPerUnit}"
					items="{
						path: 'per>/items'
					}">
					<core:Item key="{per>text}" text="{per>text}" />
				</Select> -->
				<Input
					id="idPricePerUnit"
					type="Number"
					value="{productModel>/PricePerUnit}">
				</Input>





			</f:content>
		</f:SimpleForm>

 		</semantic:content>
		<semantic:customFooterContent>
				<Button text="{viewModel>/buttonText}" type="Accept" press="SaveProduct"/>
				<Button text="Delete" type="Reject" press="deleteProduct" enabled="{viewModel>/deleteEnabled}"/>
				<Button text="Clear" type="Ghost" press="clearProduct"/>
		</semantic:customFooterContent>
	</semantic:FullscreenPage>

</mvc:View>
